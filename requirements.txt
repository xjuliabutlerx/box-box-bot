langchain==1.3.17
langchain-anthropic==1.6.1
langchain-text-splitters==1.1.2
langgraph==1.2.11
langsmith

fastf1==3.8.3

chromadb==1.5.9
langchain-chroma
fastembed==0.8.0

pyyaml==6.0.3
python-dotenv

streamlit

# so `pip install -r requirements.txt` alone (what Streamlit Cloud runs)
# also makes box_box_bot importable, without a separate `pip install -e .` step
-e .
