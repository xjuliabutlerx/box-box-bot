[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "box-box-bot"
version = "0.1.0"
description = "Conversational F1 chatbot agent (LangGraph + fastf1 + RAG)"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
